package org.firstinspires.ftc.teamcode.controllers.profiled;

public class ProfiledPDFLController {
    // looks like a profiled pdfl controller will have Kp, Kd (for the pidoutput), but also a kV, kA term
//    double targetPosition = profile.getPosition(time);
//    double targetVelocity = profile.getVelocity(time);
//    double targetAcceleration = profile.getAcceleration(time); // optional
//
//    double measuredPosition = encoder.getPosition();
//    double measuredVelocity = encoder.getVelocity(); // optional
//
//    // PID controls position error
//    double positionError = targetPosition - measuredPosition;
//    double pidOutput = pid.calculate(measuredPosition, targetPosition);
//
//    // Feedforward helps track desired velocity/acceleration
//    double feedforward = kV * targetVelocity + kA * targetAcceleration;
//
//    // Total motor output
//    double output = pidOutput + feedforward;
//motor.set(output);

//while (TrajectoryIsNotDone) {
//
//        double x = motion_profile_position(max_acceleration, max_velocity, distance, elapsed_time);
//        double v = motion_profile_velo(max_acceleration, max_velocity, distance, elapsed_time);
//        double a = motion_profile_accel(max_acceleration, max_velocity, distance, elapsed_time);
//
//        double motorPower = (x - motor.getPosition()) * Kp + Kv * v + Ka * a;
//    }
}
